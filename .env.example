# Copie ce fichier en ".env" (sans ".example") et renseigne ta vraie clé.
# Le fichier ".env" ne doit JAMAIS être commité — vérifie qu'il est bien
# dans .gitignore avant tout `git add`.
ANTHROPIC_API_KEY=
